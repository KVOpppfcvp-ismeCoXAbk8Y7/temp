#!/usr/bin/bash

set -eu

WDIR="$(pwd)"
ROOT="$(dirname "$(dirname "$(realpath "${BASH_SOURCE[0]}")")")"

cleanup() {
  local status=$?

  pushd "$WDIR"
  exit $status
}

main() {
  trap cleanup EXIT
  pushd "$ROOT"

  docker logs "$@"
}

main "$@"
