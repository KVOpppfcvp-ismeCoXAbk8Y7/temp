{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "type": "tsc --noEmit",
    "dev": "sh bin/start dev",
    "prod": "sh bin/start prod",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host ${HOST} --port ${PORT_PREVIEW:4173}"
  }
}
